# 智能灯杆监控系统 - GitHub 提交指南

## 前提要求

1. ✅ GitHub 仓库已创建: https://github.com/zephyrnan/Intelligent-lamp-post-monitoring-system
2. ✅ Git 用户信息已配置
3. ✅ 有仓库的推送权限

## 详细步骤

### 步骤 1: 创建项目根目录并初始化 Git

```bash
# 在合适的位置创建项目根目录
cd C:\Users\hhn\Desktop
mkdir Intelligent-lamp-post-monitoring-system
cd Intelligent-lamp-post-monitoring-system

# 初始化 Git 仓库
git init
git branch -M main
```

### 步骤 2: 整理项目结构

```bash
# 创建 frontend 和 backend 目录
mkdir frontend
mkdir backend

# 复制前端项目（不包括 .git 和 node_modules）
cd "C:\Users\hhn\Desktop\vue3\智能灯杆监控系统\room-monitor"
# 手动复制所有文件到 C:\Users\hhn\Desktop\Intelligent-lamp-post-monitoring-system\frontend
# 排除: .git, node_modules, dist

# 复制后端项目
cd "E:\ideaProject\mongdb\roomMonitor"
# 手动复制所有文件到 C:\Users\hhn\Desktop\Intelligent-lamp-post-monitoring-system\backend
# 排除: node_modules
```

**重要提示**: 使用 Windows 资源管理器或以下命令复制：

```powershell
# 使用 PowerShell 复制（推荐）

# 复制前端（排除不需要的文件夹）
robocopy "C:\Users\hhn\Desktop\vue3\智能灯杆监控系统\room-monitor" "C:\Users\hhn\Desktop\Intelligent-lamp-post-monitoring-system\frontend" /E /XD node_modules dist .git .claude

# 复制后端（排除不需要的文件夹）
robocopy "E:\ideaProject\mongdb\roomMonitor" "C:\Users\hhn\Desktop\Intelligent-lamp-post-monitoring-system\backend" /E /XD node_modules .idea
```

### 步骤 3: 复制主 README

```bash
cd C:\Users\hhn\Desktop\Intelligent-lamp-post-monitoring-system

# 复制主 README
copy "C:\Users\hhn\Desktop\vue3\智能灯杆监控系统\README.md" .
```

### 步骤 4: 创建根目录 .gitignore

在项目根目录创建 `.gitignore`:

```bash
# 在 C:\Users\hhn\Desktop\Intelligent-lamp-post-monitoring-system 目录下
notepad .gitignore
```

添加以下内容：

```
# Node modules
**/node_modules/

# Build outputs
**/dist/
**/build/

# Logs
**/*.log
logs/

# Environment
**/.env
**/.env.local

# IDE
.idea/
.vscode/
**/.DS_Store

# Claude
**/.claude/

# OS
Thumbs.db
*.swp
*.swo
```

### 步骤 5: 添加所有文件到 Git

```bash
cd C:\Users\hhn\Desktop\Intelligent-lamp-post-monitoring-system

# 查看将要提交的文件
git status

# 添加所有文件
git add .

# 查看暂存的文件
git status
```

### 步骤 6: 创建初始提交

```bash
git commit -m "feat: 初始提交 - 智能灯杆监控系统

- ✨ 添加 Vue 3 前端应用
- ✨ 添加 Node.js + Express 后端服务
- ✨ 实现实时监控功能
- ✨ 实现报警管理系统
- ✨ 实现人员检测功能
- ✨ 实现历史数据分析
- ✨ 添加完整的项目文档

🤖 Generated with Claude Code"
```

### 步骤 7: 关联远程仓库

```bash
# 添加远程仓库
git remote add origin https://github.com/zephyrnan/Intelligent-lamp-post-monitoring-system.git

# 验证远程仓库
git remote -v
```

### 步骤 8: 推送到 GitHub

```bash
# 推送到主分支
git push -u origin main
```

**如果遇到认证问题**：

```bash
# 方式 1: 使用 Personal Access Token (推荐)
# 1. 访问 GitHub: Settings -> Developer settings -> Personal access tokens -> Tokens (classic)
# 2. 生成新 token，选择 repo 权限
# 3. 复制 token
# 4. 推送时使用 token 作为密码

git push -u origin main
# Username: zephyrnan
# Password: [粘贴你的 personal access token]

# 方式 2: 使用 SSH
# 1. 生成 SSH 密钥
ssh-keygen -t ed25519 -C "2764304248@qq.com"

# 2. 添加到 GitHub: Settings -> SSH and GPG keys -> New SSH key
cat ~/.ssh/id_ed25519.pub

# 3. 修改远程仓库地址为 SSH
git remote set-url origin git@github.com:zephyrnan/Intelligent-lamp-post-monitoring-system.git

# 4. 推送
git push -u origin main
```

### 步骤 9: 验证提交

访问您的 GitHub 仓库：
https://github.com/zephyrnan/Intelligent-lamp-post-monitoring-system

确认以下内容：
- ✅ 前端和后端代码都已上传
- ✅ README.md 正确显示
- ✅ 文件结构清晰

## 后续开发流程

### 日常开发

```bash
# 1. 修改代码后查看状态
git status

# 2. 添加修改的文件
git add .
# 或添加特定文件
git add frontend/src/views/NewView.vue

# 3. 提交修改
git commit -m "feat: 添加新功能"

# 4. 推送到 GitHub
git push
```

### 提交信息规范

使用约定式提交（Conventional Commits）：

```bash
# 新功能
git commit -m "feat: 添加用户管理功能"

# 修复 bug
git commit -m "fix: 修复登录页面样式问题"

# 文档更新
git commit -m "docs: 更新 API 文档"

# 性能优化
git commit -m "perf: 优化数据查询性能"

# 重构代码
git commit -m "refactor: 重构报警模块代码"

# 样式调整
git commit -m "style: 统一代码格式"

# 测试
git commit -m "test: 添加单元测试"

# 构建/依赖
git commit -m "chore: 更新依赖包版本"
```

## 常见问题解决

### 问题 1: Push 被拒绝（rejected）

```bash
# 原因：远程仓库有新的提交
# 解决：先拉取再推送
git pull origin main --rebase
git push origin main
```

### 问题 2: 认证失败

```bash
# 解决：使用 Personal Access Token 或 SSH
# 见步骤 8 中的认证说明
```

### 问题 3: 文件过大

```bash
# 如果有大文件（>100MB），使用 Git LFS
git lfs install
git lfs track "*.mp4"
git add .gitattributes
```

### 问题 4: 忘记添加 .gitignore 导致提交了 node_modules

```bash
# 从 Git 中移除但保留本地文件
git rm -r --cached node_modules
git commit -m "chore: 移除 node_modules"
git push
```

## 团队协作

### 分支管理

```bash
# 创建开发分支
git checkout -b develop

# 创建功能分支
git checkout -b feature/user-management

# 合并分支
git checkout main
git merge feature/user-management

# 删除分支
git branch -d feature/user-management
```

### Pull Request 流程

1. Fork 仓库
2. 创建功能分支
3. 提交修改
4. 推送到 Fork 的仓库
5. 在 GitHub 上创建 Pull Request
6. 代码审查
7. 合并到主分支

## 补充说明

### 项目文档结构

```
Intelligent-lamp-post-monitoring-system/
├── README.md                 # 主文档（已创建）
├── LICENSE                   # 许可证（可选）
├── .gitignore               # Git 忽略规则
├── frontend/                # 前端项目
│   ├── src/
│   ├── public/
│   ├── package.json
│   └── README.md            # 前端文档（已创建）
└── backend/                 # 后端项目
    ├── routes/
    ├── module/
    ├── package.json
    └── README.md            # 后端文档（已创建）
```

### 推荐的 GitHub 仓库设置

1. **About 部分**
   - Description: 智能灯杆监控系统 - 基于 Vue 3 + Node.js 的物联网监控平台
   - Website: 部署后的网站地址
   - Topics: vue3, nodejs, mongodb, iot, monitoring-system, websocket

2. **分支保护**
   - Settings -> Branches -> Add rule
   - 保护 main 分支
   - 要求 PR 审查后才能合并

3. **Issues 模板**
   - 创建 bug 报告模板
   - 创建功能请求模板

4. **GitHub Actions**（可选）
   - 自动化测试
   - 自动部署

## 需要帮助？

如果遇到问题：
1. 查看 Git 官方文档: https://git-scm.com/doc
2. 查看 GitHub 帮助: https://docs.github.com
3. 在项目中提 Issue

---

祝您提交顺利！🎉
