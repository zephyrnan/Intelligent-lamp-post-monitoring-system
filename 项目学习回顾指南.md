





# 智能灯杆物联网监控平台 - 项目学习回顾指南

> 这份文档帮助你快速回忆整个项目的架构、数据流、核心实现细节。

---

## 一、项目整体架构

```
Intelligent-lamp-post-monitoring-system/
├── frontend/                    # Vue 3 前端
│   └── src/
│       ├── views/               # 7 个页面
│       │   ├── RoomList.vue         # 灯杆列表（首页）
│       │   ├── RoomDetail.vue       # 灯杆详情（传感器数据 + 视频 + 设备控制）
│       │   ├── AlarmList.vue        # 告警列表
│       │   ├── HistoryData.vue      # 历史数据分析
│       │   ├── DetectionHistory.vue # 人员检测记录
│       │   ├── HomeView.vue         # 默认首页
│       │   └── NotFound.vue         # 404 页面
│       ├── components/
│       │   ├── charts/BaseChart.vue            # 通用图表组件（核心）
│       │   ├── common/DeviceControl.vue        # 设备远程控制
│       │   ├── common/RoomCard.vue             # 灯杆卡片
│       │   ├── common/StatusCard.vue           # 状态卡片
│       │   ├── layout/AppLayout.vue            # 全局布局
│       │   └── WebSocketVideoStream.vue        # 实时视频流
│       ├── stores/modules/      # 4 个 Pinia Store
│       │   ├── websocketStore.ts    # WebSocket 连接管理
│       │   ├── roomStore.ts         # 灯杆数据管理
│       │   ├── alarmStore.ts        # 告警状态管理
│       │   └── historyStore.ts      # 历史数据管理
│       ├── api/                 # API 接口层
│       │   ├── realRoomApi.ts       # 真实灯杆 API（核心）
│       │   ├── alarmApi.ts          # 告警 API
│       │   ├── historyApi.ts        # 历史数据 API
│       │   ├── detectionApi.ts      # 人员检测 API
│       │   ├── deviceApi.ts         # 设备控制 API
│       │   ├── roomApi.ts           # Mock 房间 API
│       │   └── index.ts             # 统一导出
│       ├── types/index.ts       # 所有 TypeScript 类型定义
│       ├── router/index.ts      # 路由配置
│       └── utils/               # 工具函数
│
└── backend/                     # Node.js 后端
    ├── app.js                       # Express 应用入口
    ├── bin/www                      # 启动入口
    ├── routes/                      # 路由
    │   ├── rooms.js                 # 灯杆数据路由
    │   ├── roomslist.js             # 历史数据路由
    │   ├── alarms.js                # 告警路由
    │   └── detections.js            # 检测路由
    ├── module/                      # Mongoose 模型
    │   ├── Rooms.js                 # 灯杆模型
    │   ├── RoomsList.js             # 历史记录模型
    │   ├── Alarm.js                 # 告警模型
    │   └── PersonDetection.js       # 检测记录模型
    └── services/
        └── videoStreamService.js    # 视频流服务
```

---

## 二、数据流全链路

### 2.1 实时数据流（传感器 → 页面显示）

```
硬件传感器
    ↓ (串口/网络上报)
后端 Express 服务 (localhost:3000)
    ↓ MongoDB 持久化
    ↓ Socket.IO 实时推送事件: room:update / alarm:new
    ↓
前端 WebSocket Store (websocketStore.ts)
    ↓ 事件分发
前端 Room Store (roomStore.ts) ← 更新灯杆数据
    ↓
页面组件 (RoomDetail.vue)
    ↓
BaseChart 图表组件 ← 200ms 节流更新 ECharts
```

### 2.2 HTTP 请求流（页面操作 → API → 数据库）

```
页面组件
    ↓ 调用 api/ 层方法
realRoomApi.ts (数据转换：后端字符串 → 前端数字类型)
    ↓ Axios HTTP 请求
后端 Express 路由 (routes/)
    ↓
Mongoose 模型 (module/)
    ↓
MongoDB 数据库 (roomMonitor)
```

---

## 三、核心技术点详解

### 3.1 WebSocket 指数退避重连（websocketStore.ts）

**问题**：弱网环境下设备频繁断连，固定间隔重连会造成服务器压力。

**方案**：自定义重连逻辑，禁用 Socket.IO 内置重连。

```typescript
// 关键配置
const config = {
  reconnection: false,       // 禁用内置重连，使用自定义逻辑
  reconnectInterval: 1000,   // 基础延迟 1s
  maxReconnectAttempts: 10   // 最大重连 10 次
}

// 核心算法：指数退避 + 随机抖动
function getReconnectDelay(attempt: number): number {
  // 延迟 = min(1s * 2^attempt, 30s)
  const delay = Math.min(
    config.reconnectInterval * Math.pow(2, attempt),
    MAX_RECONNECT_DELAY  // 30000ms
  )
  // 附加 ±20% 随机抖动，防止多设备同时重连（惊群效应）
  const jitter = delay * 0.2 * (Math.random() * 2 - 1)
  return Math.round(delay + jitter)
}
```

**重连时间线**（不含抖动）：
| 次数 | 延迟 |
|------|------|
| 第1次 | 1s |
| 第2次 | 2s |
| 第3次 | 4s |
| 第4次 | 8s |
| 第5次 | 16s |
| 第6~10次 | 30s（上限） |

**触发时机**：
- `connect_error` 事件 → 调用 `scheduleReconnect()`
- `disconnect` 事件 → 调用 `scheduleReconnect()`
- 手动 `disconnect()` → 调用 `cancelReconnect()` 取消定时器，重置计数

**文件位置**：`frontend/src/stores/modules/websocketStore.ts`

---

### 3.2 告警状态机（alarmStore.ts）

**问题**：告警状态随意跳转会导致数据混乱（比如直接从 active 跳到 resolved）。

**方案**：定义严格的状态转换表，任何操作前先校验合法性。

```typescript
// 状态只能单向前进，不能跳过或回退
type AlarmStatus = 'active' | 'acknowledged' | 'resolved'

const VALID_TRANSITIONS: Record<AlarmStatus, AlarmStatus[]> = {
  active:       ['acknowledged'],  // 活跃 → 只能确认
  acknowledged: ['resolved'],      // 已确认 → 只能解决
  resolved:     []                 // 已解决 → 终态，不能再转
}

function isValidTransition(from: AlarmStatus, to: AlarmStatus): boolean {
  return VALID_TRANSITIONS[from]?.includes(to) ?? false
}
```

**使用场景**：
```typescript
// acknowledgeAlarm() 中
if (!isValidTransition(alarm.status, 'acknowledged')) {
  throw new Error(`非法状态转换: ${alarm.status} → acknowledged`)
}

// resolveAlarm() 中
if (!isValidTransition(alarm.status, 'resolved')) {
  throw new Error(`非法状态转换: ${alarm.status} → resolved`)
}
```

**告警全生命周期**：
```
传感器异常 → 创建告警(active) → 运维确认(acknowledged) → 问题解决(resolved)
                ↓                    ↓                       ↓
             可见通知              记录确认人/时间           记录解决时间
```

**文件位置**：`frontend/src/stores/modules/alarmStore.ts`

---

### 3.3 泛型传感器阈值配置（types/index.ts + RoomDetail.vue）

**问题**：4 类传感器的告警判断逻辑重复，阈值硬编码散落各处。

**方案**：TypeScript 泛型 + 配置表驱动。

```typescript
// 1. 类型定义（types/index.ts）
type SensorType = 'temperature' | 'humidity' | 'smoke' | 'water'
type AlarmLevel = 'critical' | 'high' | 'medium' | 'low'

interface SensorThreshold<T extends SensorType = SensorType> {
  type: T           // 泛型约束：传感器类型
  label: string     // 中文名
  unit: string      // 单位
  levels: Record<AlarmLevel, ThresholdRange>  // 四级阈值范围
}

// 映射类型：确保配置表包含所有传感器类型
type SensorThresholdConfig = {
  [K in SensorType]: SensorThreshold<K>
}
```

```typescript
// 2. 配置表（RoomDetail.vue）
const sensorThresholds: SensorThresholdConfig = {
  temperature: {
    type: 'temperature', label: '温度', unit: '°C',
    levels: {
      critical: { min: 18, max: 28 },   // 超出 18~28 → 严重
      high:     { min: 20, max: 26 },   // 超出 20~26 → 高
      medium:   { min: 22, max: 25 },   // 超出 22~25 → 中
      low:      {}                       // 其余 → 低
    }
  },
  smoke: {
    type: 'smoke', label: '烟雾浓度', unit: '',
    levels: {
      critical: { max: 50 },    // 单侧阈值：只有上限
      high:     { max: 30 },
      medium:   { max: 20 },
      low:      {}
    }
  }
  // ... humidity, water 类似
}
```

```typescript
// 3. 通用判断函数
function evaluateSensorLevel(value, threshold): AlarmLevel | null {
  // 从高到低检查：critical → high → medium
  for (const level of ['critical', 'high', 'medium']) {
    const range = threshold.levels[level]
    // 支持三种模式：
    // 双侧阈值（min + max）：温度、湿度
    // 单侧上限（max only）：烟雾、水位
    // 单侧下限（min only）：如有需要
    if (range.min && range.max) {
      if (value < range.min || value > range.max) return level
    } else if (range.max) {
      if (value > range.max) return level
    }
  }
  return null  // 正常范围
}
```

**好处**：新增传感器只需在配置表加一项，不用改判断逻辑。

---

### 3.4 BaseChart 图表组件（components/charts/BaseChart.vue）

**三个核心优化**：

#### (1) IntersectionObserver 懒加载

```typescript
// 原理：图表不在可视区域时不渲染，节省首屏性能
const isVisible = ref(false)

onMounted(() => {
  observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          isVisible.value = true  // 进入视口才开始渲染
        }
      })
    },
    { threshold: 0.1 }  // 10% 可见即触发
  )
  observer.observe(chartWrapperRef.value)
})
```

模板中配合 `v-if`：
```html
<v-chart v-if="isVisible && !loading && chartData" :option="throttledChartOption" />
```

#### (2) setOption 节流（200ms）

```typescript
// 原理：传感器数据高频更新时，限制 ECharts 重绘频率
const THROTTLE_DELAY = 200
const throttledChartOption = shallowRef({})  // shallowRef 减少深层响应开销

watch(() => chartOption.value, (newOption) => {
  const now = Date.now()
  if (now - lastUpdateTime >= THROTTLE_DELAY) {
    // 距上次更新超过 200ms，立即更新
    throttledChartOption.value = newOption
    lastUpdateTime = now
  } else {
    // 200ms 内的更新，延迟到时间窗口末尾批量更新
    if (throttleTimer) clearTimeout(throttleTimer)
    throttleTimer = setTimeout(() => {
      throttledChartOption.value = chartOption.value
      lastUpdateTime = Date.now()
    }, THROTTLE_DELAY - (now - lastUpdateTime))
  }
})
```

#### (3) 统一三种图表类型

```typescript
interface Props {
  type: 'line' | 'bar' | 'pie'  // 通过 prop 切换图表类型
  data: any                      // 统一数据格式 { labels, datasets }
  showDataZoom?: boolean         // 是否显示滑动缩放
  showLegend?: boolean           // 是否显示图例
}
```

组件内部根据 `type` 自动构建对应的 ECharts option，外部调用者只需传入统一格式数据。

**文件位置**：`frontend/src/components/charts/BaseChart.vue`

---

## 四、API 层数据转换（realRoomApi.ts）

这是前端最关键的一个 API 文件，负责**将后端字符串数据转换为前端数字类型**。

### 4.1 为什么需要转换？

后端 MongoDB 存储的是字符串格式（硬件上报的原始数据）：
```json
{ "smoke": "23", "temp": "25.5", "water": "0", "warn": "0", "hum": "65" }
```

前端需要数字格式才能做图表渲染和阈值判断。

### 4.2 核心转换逻辑

```typescript
// 房间 ID 映射（后端用数字 ID，前端用 room01 格式）
const roomIdMap: Record<string, string> = {
  '1': 'room01', '2': 'room02', ...
}

// 房间名称配置
const roomConfig: Record<string, { name: string; location: string }> = {
  '1': { name: '灯杆01', location: 'A区' },
  '2': { name: '灯杆02', location: 'B区' },
}

// 后端 RealRoomData → 前端 RoomInfo
function transformToRoomInfo(data: RealRoomData): RoomInfo {
  const warnLevel = parseInt(data.warn) || 0
  return {
    id: roomIdMap[data.roomId] || `room${data.roomId.padStart(2, '0')}`,
    name: roomConfig[data.roomId]?.name || `灯杆${data.roomId}`,
    status: warnLevel === 0 ? 'normal' : warnLevel === 1 ? 'warning' : 'error',
    temperature: parseFloat(data.temp) || 0,
    humidity: parseFloat(data.hum) || 0,
    // ...
  }
}

// 后端 RealRoomData → 前端 SensorData
function transformToSensorData(data: RealRoomData): SensorData {
  return {
    smokeLevel: parseFloat(data.smoke) || 0,
    temperature: parseFloat(data.temp) || 0,
    hum: parseFloat(data.hum) || 0,
    lux: parseFloat(data.lux) || 0,
    sc: parseFloat(data.sc) || 0,
    sv: parseFloat(data.sv) || 0,
    bv: parseFloat(data.bv) || 0,
    // ...
  }
}
```

**文件位置**：`frontend/src/api/realRoomApi.ts`

---

## 五、路由结构（router/index.ts）

```typescript
const routes = [
  { path: '/',                  component: RoomList },        // 灯杆列表
  { path: '/room/:id',         component: RoomDetail,        // 灯杆详情
    props: true },             // 将 :id 作为 props 传入组件
  { path: '/alarms',           component: AlarmList },        // 告警管理
  { path: '/history',          component: HistoryData },      // 历史数据
  { path: '/detection-history', component: DetectionHistory }, // 检测记录
  { path: '/:pathMatch(.*)*',  component: NotFound }          // 404
]
```

所有页面组件都使用**路由懒加载**：`() => import('../views/xxx.vue')`

---

## 六、4 大 Pinia Store 职责

| Store | 文件 | 职责 |
|-------|------|------|
| **websocketStore** | `stores/modules/websocketStore.ts` | Socket.IO 连接管理、事件监听/发送、指数退避重连、心跳检测 |
| **roomStore** | `stores/modules/roomStore.ts` | 灯杆列表加载、单个灯杆详情、实时数据更新、状态变更 |
| **alarmStore** | `stores/modules/alarmStore.ts` | 告警 CRUD、状态机流转（active→acknowledged→resolved）、分级告警 |
| **historyStore** | `stores/modules/historyStore.ts` | 历史传感器数据查询、时间范围筛选、数据聚合 |

Store 之间的依赖关系：
```
websocketStore（底层通信）
    ↕ 事件驱动
roomStore（灯杆数据）     alarmStore（告警数据）
    ↕                       ↕
historyStore（历史数据）
```

---

## 七、后端结构（backend/）

### 7.1 应用入口 (app.js)

```javascript
// MongoDB 连接
mongoose.connect('mongodb://localhost:27017/roomMonitor')

// CORS 配置（允许前端跨域）
app.use(cors())

// 4 个路由模块
app.use('/rooms', roomsRouter)           // 灯杆实时数据
app.use('/roomslist', roomslistRouter)   // 历史数据
app.use('/detections', detectionsRouter) // 人员检测
app.use('/alarms', alarmsRouter)         // 告警管理
```

### 7.2 数据模型 (module/)

| 模型 | 集合 | 用途 |
|------|------|------|
| Rooms | rooms | 灯杆实时传感器数据 |
| RoomsList | roomslists | 历史传感器记录 |
| Alarm | alarms | 告警记录 |
| PersonDetection | persondetections | 人员检测结果 |

### 7.3 API 端点速查

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/rooms` | 所有灯杆数据 |
| GET | `/rooms/:id` | 单个灯杆 |
| GET | `/rooms/:id/device/control` | 设备控制状态 |
| POST | `/rooms/:id/device/control` | 远程设备控制 |
| GET | `/roomslist` | 历史数据查询 |
| GET | `/alarms` | 告警列表 |
| POST | `/alarms` | 创建告警 |
| PUT | `/alarms/:id/acknowledge` | 确认告警 |
| PUT | `/alarms/:id/resolve` | 解决告警 |
| POST | `/detections/detect/:roomId` | 触发检测 |
| GET | `/detections` | 检测历史 |

---

## 八、TypeScript 类型速查（types/index.ts）

```typescript
// 灯杆信息
interface Room {
  id: string
  name: string
  status: 'normal' | 'warning' | 'error' | 'offline'
  temperature: number
  humidity: number
  airQuality: number
  lastUpdateTime: string
}

// 传感器数据（8 路）
interface SensorData {
  smokeLevel: number   // 烟雾浓度
  temperature: number  // 温度
  hum: number          // 湿度
  lux: number          // 光照强度
  sc: number           // 旁路电流
  sv: number           // 旁路电压
  bv: number           // 总电压
  water?: number       // 水位
  warn?: number        // 报警状态 0/1/2
}

// 后端原始数据（全是字符串）
interface RealRoomData {
  roomId: string
  smoke: string
  temp: string
  water: string
  warn: string     // "0"/"1"/"2"
  hum: string
  lux: string
  sc: string
  sv: string
  bv: string
}

// 告警信息
interface AlarmInfo {
  id: string
  roomId: string
  type: 'smoke' | 'temperature' | 'water' | ...
  level: 'critical' | 'high' | 'medium' | 'low'
  status?: 'active' | 'acknowledged' | 'resolved'
  message: string
  timestamp: string
}

// 泛型阈值配置
interface SensorThreshold<T extends SensorType> {
  type: T
  label: string
  unit: string
  levels: Record<AlarmLevel, ThresholdRange>
}
```

---

## 九、Socket.IO 事件速查

| 事件名 | 方向 | 数据 | 说明 |
|--------|------|------|------|
| `room:update` | 服务端→前端 | `{ roomId, sensorData }` | 灯杆传感器数据更新 |
| `alarm:new` | 服务端→前端 | `AlarmInfo` | 新告警推送 |
| `device:status` | 双向 | `{ roomId, deviceId, status }` | 设备状态变更 |
| `detection:result` | 服务端→前端 | `PersonDetection` | 检测结果推送 |
| `video_frame` | 服务端→前端 | `{ roomId, frame }` | 视频帧数据 |
| `join_room` | 前端→服务端 | `roomId` | 加入灯杆房间 |
| `leave_room` | 前端→服务端 | `roomId` | 离开灯杆房间 |

---

## 十、快速启动

```bash
# 1. 启动 MongoDB（确保已安装）
mongod

# 2. 启动后端
cd backend
npm install
npm start
# → http://localhost:3000

# 3. 启动前端（新终端）
cd frontend
npm install
npm run dev
# → http://localhost:5173
```

---

## 十一、面试高频问答

### Q1: 为什么用指数退避而不是固定间隔重连？
固定间隔在大量设备同时断连时会造成"重连风暴"（惊群效应）。指数退避让每次重连间隔翻倍，加上随机抖动让不同设备错开重连时间，减轻服务器瞬时压力。

### Q2: 状态机解决了什么问题？
防止告警状态的非法跳转。比如一个 active 告警不能直接变成 resolved（跳过了确认步骤），也不能从 resolved 回退到 active。状态机用转换表白名单方式，只允许合法的单向流转。

### Q3: 为什么用 shallowRef 而不是 ref？
ECharts option 是一个深层嵌套的大对象，用 `ref` 会对其所有层级做响应式代理（Proxy），造成不必要的性能开销。`shallowRef` 只对顶层引用做响应式，配合手动赋值触发更新，适合大对象场景。

### Q4: IntersectionObserver 和 v-if 配合的好处？
页面可能有多个图表，如果全部同时初始化 ECharts 实例会阻塞主线程。IntersectionObserver 检测元素是否进入视口，配合 `v-if` 做到"滚动到才渲染"，减少首屏渲染压力。

### Q5: 泛型阈值配置的好处？
用 `SensorThreshold<T extends SensorType>` 泛型约束，配合映射类型 `[K in SensorType]: SensorThreshold<K>` 确保每个传感器类型都有对应配置（编译期检查）。新增传感器只需在 `SensorType` 加类型 + 配置表加一条，判断逻辑完全复用。

### Q6: 前端为什么需要做数据转换（realRoomApi.ts）？
硬件上报的数据是字符串格式，而前端图表渲染和阈值判断需要数字类型。在 API 层统一转换（`parseFloat`），上层业务组件不用关心数据格式问题，保持单一职责。
